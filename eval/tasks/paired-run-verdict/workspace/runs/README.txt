One JSON file per run, named <arm>-<sample>.json.

arm      which side of the pair the run belongs to, "with" or "without"
sample   the sample index, the same index in both arms is the same input
score    0 to 100, produced by the scorer
exit     the worker's exit code
seconds  wall clock
note     whatever the worker had to say
