# thread 9a02de, billing export

Closed on 2026-02-19. The nightly export runs, the CSV schema is frozen and
the finance side signed it off. Nothing here is open.

To reopen it, which you should not need to:

    bsr-resume 9a02de --from step-12
