# 2026-03-11

Vault checkpoint table applied on the dev database. Migration is reversible,
tested the down path twice.

- vault: checkpoint table live on dev, migration is reversible
- vault: the writer still writes checkpoints synchronously, which costs about
  40ms per batch, needs to move off the hot path
- relay: no progress, still waiting on staging credentials
- feeder: no changes

Staging credentials still not here.
