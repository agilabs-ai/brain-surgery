# 2026-03-14

Last day before I go away.

- feeder: added the duplicate key error message, points at the offending line
- vault: checkpoint writes are async now on a branch, not merged, no benchmark
  yet
- relay: still the one real hole. Soak test drops the last batch roughly one
  run in five and I do not know why. My guess is the connection recycler races
  with the flush, but that is a guess.
- credentials for staging arrived this morning, unused so far

If someone picks this up: the relay soak test is the thing that matters. The
retry cap is set to 8 in config/relay.yaml and I suspect it should be 3, which
would make the race far easier to reproduce.
