# 2026-03-09

Relay day. The retry loop counts attempts per connection and not per batch,
which is why the last batch of a long run silently disappears when the
connection is recycled. Rewrote the counter to live on the batch.

- relay: retry counter moved onto the batch
- relay: soak test still drops the last batch about one run in five, so the
  counter was not the only bug
- vault: wrote the schema for the new checkpoint table, not applied anywhere

Waiting on the platform team for the staging credentials so the soak test can
run against something that is not my laptop. Asked on Friday, no answer yet.
