One manifest per build.

published     true when that build is the one that actually went out
render_path   where the renderer left its working copy, wiped on every reboot
archive_path  where the build is kept
